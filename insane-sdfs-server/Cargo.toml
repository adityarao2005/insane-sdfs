[package]
name = "insane-sdfs-server"
version = "0.1.0"
edition = "2024"

[dependencies]
insane-sdfs-core = { path = "../insane-sdfs-core" }
tokio = { workspace = true, features = ["full"] }
libp2p = { workspace = true, features = [ "tokio", "gossipsub", "mdns", "tcp", "macros", "noise", "yamux", "quic" ] }
futures = { workspace = true }
